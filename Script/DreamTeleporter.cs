using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DreamTeleporter : MonoBehaviour
{
    //Add a ScriptableObject as a teleporter point. Global access It should refer to the name of the zone and refer some position roughly
    public List<ScriptableObject> TeleportationPoints = new List<ScriptableObject>();
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    public void teleportOnSameScene()
    {

    }

    public void teleportOnOtherScene()
    {

    }
}
